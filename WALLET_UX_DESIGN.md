# PaidAhead 고객 인증·지갑 UX 설계

개정: 2026-09-20 · 상태: 후속 설계안. **내장 지갑·실제 인증·가스비 대납·복구 기능은 미구현**이다. 제공자 SDK, 계약, DB 마이그레이션을 이 문서 개정으로 변경하지 않는다.

관련 문서: [기능명세서](PaidAhead_기능명세서_v1.md), [ERD](ERD.md), [컴포넌트 구조](COMPONENT_ARCHITECTURE.md), [랜딩](landing.md), [제안 요약](proposal_digest.md).

## 1. 방향과 현재 경계

납품업체·구매처는 지갑 브랜드, 개인키, 네트워크 추가, 가스 토큰을 배우지 않고 자신의 거래를 확인할 수 있어야 한다. 기본 화면에는 받을/갚을 금액, 상대방, 만기, 현재 상태, 다음 행동을 우선한다. 연령·지역만으로 익숙함을 단정하지 않고 실제 대상 사용자에게 검증한다.

| 구분 | 현재 시연 | 후속 목표 |
|---|---|---|
| 접근 | 시연 역할 선택·조직 범위 API | 실제 사용자 인증 + 승인된 조직 소속·업무 역할 |
| 거래 서명 | 외부 EOA 지갑 연결 및 실제 승인/거래 서명 | 로그인에 연결된 스마트 계정과 거래별 패스키 인증 |
| 화면 | 하단 시연 지갑 설정 접기, 거래 시 연결 안내, 기술 진행 펼쳐보기 | 고객 가입 과정에서 외부 지갑 설치·브랜드 선택 제거 |
| 비용 | 시연 계정의 네이티브 가스·모의 토큰 필요 | 허용된 거래의 네트워크 비용 대납 |
| 복구 | 현재 지갑 제공자의 계정 접근에 의존 | 검토·유예·통지·온체인 권한 변경을 갖춘 복구 |

현재는 은행 오퍼·업체 수락·구매처 상환에 외부 지갑이 필요하다. ‘시연 지갑’ 표기를 유지하고 연결을 본인확인이나 실제 은행 인증이라고 설명하지 않는다. 토큰 사용 승인이 필요하면 별도 거래다. 현재 클릭 수와 서명 횟수가 미래 목표와 같다고 주장하지 않는다. 파일 업로드·AI 서류 비교도 여전히 목표 범위이며 현 시연의 수동 서류 검토와 구분한다.

## 2. 사용자 흐름과 명시적 승인

1. 실제 인증 후 승인된 조직을 선택한다. 서버는 매 요청의 활성 회원 소속·업무 역할을 검사한다. 지갑 보유나 이메일 도메인만으로 조직·은행 권한을 부여하지 않는다.
2. 납품업체·구매처는 조직에 귀속된 결제용 스마트 계정을 사용한다. 담당자 인증 수단과 자산 보유 주소를 분리한다. 계정 생성·바인딩은 조직 승인과 주소/체인 검증이 끝난 뒤 활성화한다.
3. 거래 전 확인 화면에 조직·상대방·채권·금액·차액·만기/조건 유효기간·사용 토큰·실제/모의 여부를 표시한다. 예: ‘300만원 채권을 넘기고 297만원 상당 모의 토큰 받기’. 승인 거래가 있으면 대상 계약과 사용 한도를 함께 설명한다.
4. ‘이 조건으로 먼저받기’ 또는 ‘300만원 모의 상환’ 선택 후 패스키 사용자 확인을 요구한다. 로그인 세션만으로 자산 이동을 승인하지 않는다. 표시한 조건의 해시·체인·실행 계정·계약·함수·금액·nonce·만료를 서명 대상에 결합한다. 조건이나 바인딩 버전이 바뀌면 다시 확인·인증한다.
5. 화면은 ‘승인 필요 → 처리 중 → 모의 지급/상환 완료’를 기본으로 보여준다. 기술 상세에서 준비·지갑 승인·전송·체인 확정·DB 반영과 해시를 확인할 수 있다. 서버의 검증된 CONFIRMED 전에는 완료로 표시하지 않는다. 새로고침은 기존 작업을 복구하며 시간 초과를 즉시 실패로 단정하지 않는다.

패스키 생체 확인은 기기/인증기 안에서 처리한다. PaidAhead DB에는 생체정보·개인키·복구 비밀을 저장하지 않는다. WebAuthn challenge, origin/RP ID, 사용자 확인, 만료·재사용을 검증하고 해당 승인이 정확한 실행 요청에 결합되는지 PoC로 증명한다. 단순 패스키 로그인 성공을 체인 서명으로 간주하지 않는다. 제공자가 서버 단독으로 사용자 자산을 이전할 수 있는 구조라면 이 설계의 사용자 승인 모델을 충족한 것으로 보지 않는다.

은행은 개인 사장님용 가입 흐름과 분리한다. 기관 관리 서명 수단, 검토자·승인자·실행자의 역할 분리, 금액별 복수 승인 정책을 적용하는 어댑터를 둔다. HSM/MPC/다중서명 중 운영 방식은 기관과 선정하며 아직 도입하지 않았다. 등록 워커는 기존 등록 권한만 유지하고 사용자 매입·상환을 대신 승인하지 않는다.

## 3. 스마트 계정과 검증 어댑터

목표는 ERC-4337 계열 스마트 계정이다. 제공자·계정 구현·EntryPoint 버전·bundler·paymaster는 미선정이다. Injective EVM 테스트넷에서 사용 가능하다고 가정하지 않는다. 정확한 체인 ID, 배포 코드, RPC, 시뮬레이션, 비용, 패스키 검증 및 복구를 시험한 뒤 채택한다. [ERC-4337](https://eips.ethereum.org/EIPS/eip-4337)은 UserOperation을 bundler가 EntryPoint로 전달하는 구조를 정의한다.

현 구현의 `apps/worker/src/settlement.ts`는 `receipt.from`과 역할 지갑, `tx.from/to/input`과 준비된 작업을 비교한다. `apps/api/src/banking.ts`는 EOA 발신자의 네이티브 가스를 사전 점검한다. `contracts/src/Settlement.sol`의 역할·소유자 검사는 `msg.sender`를 기준으로 한다. 따라서 UI 지갑 SDK만 교체해서는 전환이 완료되지 않는다.

후속 `ExecutionAdapter`는 EOA와 SMART_ACCOUNT 경로를 명시적으로 구분한다. EOA 검증을 완화하거나 삭제하지 않는다. 스마트 계정 경로에서는 다음을 모두 검증한다.

- 신뢰하는 체인·EntryPoint 버전/코드에서 나온 UserOperation, `userOpHash`, 스마트 계정 `sender`, nonce와 성공 결과. 외부 트랜잭션의 `from`은 bundler일 수 있으므로 조직 지갑으로 비교하지 않는다.
- 승인된 스마트 계정의 검증 가능한 실행 호출을 해석해 목적 계약·함수·인수·토큰·금액·수취인·채권/오퍼 ID·작업 승인 해시와 대조한다. 알 수 없는 계정 실행 형식, 임의 delegatecall·추가 호출을 허용하지 않는다.
- 같은 외부 거래에 여러 UserOperation이 있어도 대상 UserOperation의 실행과 Settlement 이벤트가 정확히 연결되는 근거를 확보한다. 트레이스 또는 검증 가능한 계정 실행 이벤트 등 연결 수단을 PoC에서 정한다. 외부 거래 성공이나 같은 tx_hash만으로 완료하지 않는다.
- 예상 계약의 성공 이벤트와 자산 상태, canonical block hash·확정 기준을 확인한 뒤 DB를 원자적으로 반영한다. 이벤트 중복과 작업 중복을 각각 차단하고, 재구성 시 투영을 되돌려 재검증한다. UserOperation 접수·outer tx hash·업무 확정을 별도 단계로 저장한다.

계약에서 보이는 `msg.sender`가 승인된 스마트 계정이 되도록 호출 경로를 검증한다. 단순 relayer의 주소로 바뀌는 전송 방식은 허용하지 않는다. 계약 지갑의 메시지 서명을 사용하는 기능에는 [ERC-1271](https://eips.ethereum.org/EIPS/eip-1271) 검증을 적용하고 EOA의 주소 복구만으로 판단하지 않는다. 구체적인 계정 구현·업그레이드 권한·키 보관/이전 가능성도 채택 심사에 포함한다.

## 4. 네트워크 비용 대납

대납은 네트워크 비용만 대상으로 하며 매입·상환 원금이나 모의 토큰 잔액을 충전한 것으로 처리하지 않는다. paymaster 승인과 사용자 거래 승인은 별개다.

| 통제 | 제안 정책 |
|---|---|
| 대상 | 승인 조직·활성 바인딩·허용 체인/계약/함수/토큰만. 임의 전송·배치 추가 호출 차단 |
| 한도 | 거래 금액 상한, 승인 allowance 상한, gas 및 최대 비용, 조직/사용자/일별 예산·요청률. 수치는 PoC 측정 후 배포 설정으로 확정 |
| 실행 전 | 현재 업무 권한·상태·조건 유효성·자산 잔액 검사와 시뮬레이션, 비용 예약으로 동시 요청 예산 초과 방지 |
| 재사용 방지 | chain·EntryPoint·sender·nonce·실행 해시·만료·정책 버전에 대납 권한 결합, 멱등 예약/정산 |
| 실패·장애 | ‘거래 비용 지원을 준비 중입니다’ 등 사유와 재점검 안내. 고객에게 가스 토큰 구매/부담으로 자동 전환하지 않음 |

무제한 토큰 승인을 기본값으로 삼지 않는다. approve+실행을 한 번의 사용자 확인으로 묶는 것은 정확한 호출·한도·실패 원복을 입증한 계정 구현에서만 가능하다. 내부 호출 실패를 삼키는 배치 구현은 허용하지 않는다. 기존 EOA 시연은 별도 승인 거래 흐름을 유지한다.

## 5. 새 기기·인증 분실·조직 권한 변경

새 기기에서 기존 인증 수단을 사용할 수 있으면 정상 재인증 후 접근한다. 새 인증기 등록은 기존 강한 인증으로 승인하고 다른 등록 수단에 통지한다. 인증기를 추가해도 조직 역할이나 자산 주소를 자동 변경하지 않는다.

모든 인증 수단을 잃은 경우에는 `REQUESTED → FROZEN → REVIEWED → WAITING → APPLIED` 복구 절차를 제안한다. 반려·취소도 이력으로 남긴다. 조직 소속·담당자 신원을 별도 검토하고, 검토자와 적용자를 분리한다. 유예기간과 위험별 추가 확인 기준은 운영 전 확정하며 설정이 없으면 적용할 수 없다. 기존 연락수단·조직 관리자에게 요청/적용 예정/완료를 통지하고 이의 제기·취소 경로를 둔다.

동결 범위를 명확히 구분한다. API는 신규 거래 준비·대납·서명을 막고 미제출 승인을 폐기한다. 이미 서명·제출된 거래가 DB 동결만으로 취소되지는 않는다. 스마트 계정의 온체인 동결·키 폐기·nonce 무효화 기능을 검증하고 반영 전에는 ‘동결 요청 중’으로 표시한다. 제출 중 거래는 끝까지 추적한다. 서비스 관리자 한 명의 DB 수정으로 사용자 자산을 이동시킬 수 없도록 복구 권한과 유예를 계정 수준에서도 설계한다.

복구는 가능하면 **같은 스마트 계정 주소에서 인증 수단을 교체**한다. 바인딩 버전·키 정책 버전·변경 근거·온체인 증거를 남긴다. 조직 탈퇴자는 API 접근을 철회하는 것과 별도로 계정의 서명 권한도 철회해야 한다.

현재 EOA 채권의 공급자·지급자·보유자 주소는 과거 계약 상태다. DB `wallet_binding` 주소만 새 주소로 덮어써서는 권리·지급 의무가 옮겨지지 않는다. 기존 주소 접근을 잃으면 복구 가능하다고 보장하지 않는다. 신규 주소는 새 바인딩으로 등록하고 기존 채권 연결은 유지한다. 신규 채권만 새 주소를 쓰는 전환을 기본으로 하며, 미결 채권 이전은 허용 계약 절차와 당사자 승인·권한/잔액/오퍼 처리·감사를 별도 설계하고 검증하기 전 차단한다. 매입된 채권을 관리자 임의로 재발행하거나 은행 수취권을 변경하지 않는다.

## 6. 제안 데이터와 책임

아래는 **미구현 개념 모델**이며 실제 migration 명칭과 동일하다는 뜻이 아니다. 필드·제약은 [ERD 후속 확장](ERD.md#13-후속-설계-인증지갑-확장미구현)과 함께 관리한다.

- 인증·조직 모듈: 인증 제공자 주체, 조직 소속·업무 권한, 공개 인증기 식별정보, 폐기 시각.
- 지갑 모듈: 조직·chain·주소의 고정 바인딩, EOA/SMART_ACCOUNT 구분, 바인딩/키 정책 버전, 활성·동결·폐기 상태. 제공자 참조는 비밀키가 아니다.
- 거래 모듈: 불변 거래 조건 해시·요청자·승인자·업무 버전·바인딩 버전·서명 만료를 저장한다. 공개 체인에 전송될 서명 외의 인증 비밀을 로그/DB에 복제하지 않는다.
- 실행/동기화 모듈: userOpHash와 outer tx_hash를 분리하고, 검증한 sender·EntryPoint·실행 근거·이벤트·블록을 보존한다.
- 비용·복구 모듈: 대납 예약/실비/예산/정책 버전과 복구 검토·유예·통지·온체인 적용 증거를 감사 기록으로 보존한다.

## 7. 구현 순서와 통과 기준

1. **EOA 회귀 기준 고정:** 현재 역할별 시연, 잘못된 지갑/네트워크, 잔액 부족, 거절, 새로고침 복구, 이벤트 중복 시나리오를 유지한다. 현재 UX 정리가 미래 지갑 구현 완료를 뜻하지 않는다.
2. **분리 환경 PoC:** 가상 조직·새 채권만 사용해 Injective 대상 네트워크에서 계정 생성, 패스키 승인, 대납, UserOperation 추적, Settlement 호출을 검증한다. 미지원이면 제공자/구현 재검토 후 결정하고 고객 기능을 활성화하지 않는다.
3. **검증 어댑터·데이터 설계 확정:** EOA/AA 분리, 복수 UserOperation 매칭·변조 요청 거부·멱등성·재구성 복구를 먼저 입증한다. 스키마 migration/되돌리기와 기존 미결 채권 정책을 리뷰한다.
4. **실제 인증·권한·거래 승인 연결:** 조직 온보딩, 거래별 인증, 은행 승인 정책, 한정 대납을 feature flag 뒤에서 연결한다. 시연 역할 쿠키를 실제 인증으로 재사용하지 않는다.
5. **복구·운영·사용성 검증:** 분실/새 기기/퇴사자/복구 이의제기/제공자 장애와 예산 고갈 시험을 완료한 뒤 제한 파일럿을 검토한다.

| 합격 게이트 | 필요한 증거 |
|---|---|
| 사용성 | 대상 사용자 5명 이상의 모바일 과제 수행. 지갑 설치 설명 없이 금액·차액·상대방을 설명하고 수락/상환 완료, 혼동·오조작 기록 및 수정. 지역·연령만으로 통과 판정하지 않음 |
| 사용자 승인 | 취소·만료·금액/수취인/chain 변조·조직 전환·바인딩 교체 후 승인 재사용 모두 거부, 서버 단독 승인 불가 |
| 자산 안전 | 잘못된 계정/역할·잔액 부족·허용 외 호출 차단, 교환 원자성, 승인 한도, 중복 지급 없음 |
| 실행 검증 | bundler 주소를 고객으로 오인하지 않음, 같은 outer tx의 타 UserOperation/실패 operation 오매칭 없음, 잘못된 EntryPoint·이벤트 거부 |
| 복구 | 응답 유실·새로고침·워커 재시작·재구성에서 기존 작업 복구, 미확정 결과를 완료로 표시하지 않음 |
| 비용·운영 | 대납 예산 동시 초과·재사용·임의 호출 차단, 장애 시 무단 고객 부담 없음, 기존 EOA 경로 독립 작동 |
| 계정 복구 | 유예·복수 검토·통지·이의제기, 온체인 권한 교체와 폐기 검증, 기존 채권 주소/권리 보존 |

제공자·계약 감사 범위, 대납 수치, 복구 유예기간, 체인 확정 기준은 도입 전 결정 기록을 남긴다. 이 문서는 실제 원화 지급·은행 제휴·법적 채권 양도·상용 보안 검증의 완료를 주장하지 않는다.
