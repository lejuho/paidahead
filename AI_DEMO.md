# 가상 서류 AI 분석 시연

신청 상세에서 **가상 서류 3종 분석하기**를 누르면 서버가 OpenAI Responses API를 실제 호출한다. 기본 모델은 `gpt-4.1-mini`, `OPENAI_MODEL`로 변경 가능하다. 모델 접근 권한과 별도 API 과금이 필요하다.

## 설정

프로젝트 루트 `.env`에 `OPENAI_API_KEY`를 설정한다. 브라우저용 NEXT_PUBLIC 환경변수나 소스코드에 넣지 않는다. 실행 중인 API 프로세스만 재시작한다(체인 초기화 불필요).

```dotenv
OPENAI_API_KEY=실제_API_키
OPENAI_MODEL=gpt-4.1-mini
```

## 구현 경계

- 인증된 납품업체의 해당 신청에 연결된 기존 가상 텍스트 파일 3종만 허용한다. 파일 allowlist 및 해시를 검사한다. 임의 업로드·신청 자유입력 내용은 외부로 보내지 않는다.
- 금액·지급일·품목을 구조화 추출하고 원문 인용 근거를 대조한다. 금액/품목 합계 비교는 서버 정수 연산으로 수행한다. 추출 정확성과 진위를 보장하지 않는다.
- 기존 가상 문서에는 구체적 만기 날짜가 없고 납품서에는 단가가 없다. 따라서 모두 일치했다고 꾸미지 않고 확인 필요로 표시한다.
- AI는 신청·검토 완료·구매처 확인·지갑 거래를 수정하거나 승인하지 않는다. 담당자가 원문 확인 후 수동 검토를 완료한다.
- 15초 요청 제한, 서버 인스턴스당 동시 1개/20초 간격, 자동 재호출 없음. 키 누락·오류·한도·불완전 결과는 명시적으로 실패 처리한다.
- 결과는 화면 메모리에만 보관되며 새로고침하면 사라진다. 실패 시 성공 예시로 대체하지 않는다.
- `store:false`로 요청하지만 이는 OpenAI의 모든 로그 보관을 없앤다는 뜻이 아니다. 실제 개인정보는 이번 경로에서 취급하지 않는다.
- 제출 ZIP에서 `.env`, `.local`, API 키·지갑 개인키를 제외한다. API 없이도 기존 수동 데모는 동작한다.

## 공식 참고

- https://developers.openai.com/api/docs/guides/structured-outputs
- https://developers.openai.com/api/docs/guides/your-data
- https://developers.openai.com/api/docs/models/gpt-4.1-mini

실제 API 통신 검증과 stub 기반 오류/검증 테스트 결과를 구분해 제출한다. 실제 키 없이 실행한 테스트를 실제 AI 분석 성공으로 설명하지 않는다.

## 실제 API 검증 (2026-09-20)

사용자 설정 키로 가상 서류 3종을 실제 호출하고 브라우저 결과를 확인했다. 응답 모델은 `gpt-4.1-mini-2025-04-14`. 각 서류 금액 3,000,000원, 구체적 만기 날짜 없음, 납품서 단가 누락을 확인했다. AI 자유 요약은 누락된 단가까지 일치한다고 과장할 수 있어 기본 화면에서 제외하고 서버 점검 결과와 추출 근거를 표시한다. 이 검증은 가상 서류 한 세트에 대한 동작 확인이며 일반 문서 정확도 평가는 아니다.
